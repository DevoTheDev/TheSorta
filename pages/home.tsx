// pages/index.tsx
import { Suspense, useEffect, useState } from 'react';
import { useAPI } from '../app/contexts/useAPI';
import useAPIController from '@/app/controllers/apiController';
import Formattable from '@/app/components/formattable';

const Home = () => {

    return (
        <div>
        </div>
    );
};

export default Home;
